name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  # Silence Node 20 deprecation warning on checkout/setup-python (see GitHub changelog).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install system dependencies
        run: |
          set -euxo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            tesseract-ocr \
            libgl1 \
            libglib2.0-0 \
            libsm6 \
            libxext6 \
            libxrender1 \
            libgomp1

      - name: Install Python dependencies
        run: |
          set -euxo pipefail
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          # Replace GUI OpenCV wheel (needs libGL on Linux) with headless build for CI.
          pip install --force-reinstall opencv-python-headless

      - name: Verify imports
        run: |
          set -euxo pipefail
          python -c "import cv2; from bol.config import BOLConfig; from aha.billing import PLANS; print('imports ok')"

      - name: Run tests
        run: |
          set -euxo pipefail
          python -m pytest tests/ -q --tb=short
